node_modules/
.next/
out/
build/
# Leading slash matters: this must only ignore the database folder at the
# project root. Without it the pattern also swallows src/data/ — the question
# bank — at any depth.
/data/
*.db
*.db-journal
*.db-wal
*.db-shm
.env
.env.local
.env*.local
next-env.d.ts
*.tsbuildinfo
.DS_Store
npm-debug.log*
